# action.yml
name: 'DNS Poll'
description: 'Run DNS discovery scripts'
inputs:
  keywords:
    description: 'narrow the search'
    required: true
    default: 'banana'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.keywords }}
